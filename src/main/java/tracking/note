回朔相对于普通递归来说，最大的特点是在每层递归时都可以进行至少两个选择。如果只有一个选择，就如果递归遍历数组那样，只有线性复杂度。

因此回朔一般是递归+for的写法，其时间复杂度取决于for的n

回朔一般写法如下：
f(x){
 for(i=0;i<n;i++){
    if(...){
        f(x+1)
    }
 }
}

或是没有for循环，即面临两个选择（两个选择可以直接写不用写for),一般时间复杂度为2^n
f(x,..){
    if(...){
        f(x+1,...)
    }
    f(x+1,...)
}